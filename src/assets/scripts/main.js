/**
 * Import dependencies from node_modules
 * see commented examples below
 */
import * as bootstrap from "bootstrap";

bootstrap.Alert("Loaded bootstrap");
(function () {
  const university = "UOC";
  console.log(`Hello, ${university}!`);
})();
